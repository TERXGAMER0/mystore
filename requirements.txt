# ══════════════════════════════════════════════════════
#   requirements.txt - المكتبات المطلوبة
#   محلياً  : pip install -r requirements.txt
#   على Render: يُثبَّت تلقائياً عند كل deploy
# ══════════════════════════════════════════════════════

# ─── البوت الأساسي ────────────────────────────────────
python-telegram-bot==21.6

# ─── قاعدة البيانات Supabase ──────────────────────────
supabase==2.9.0

# ─── HTTP Client (مطلوب لـ supabase و telegram) ───────
httpx==0.27.0

# ─── قراءة .env محلياً (على Render لا تأثير له) ───────
python-dotenv==1.0.1
